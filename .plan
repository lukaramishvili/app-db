WORKING
* pass defined actions as data and receive the result
* HTTP OPTIONS request returns results of (describe-event event)
* reading and writing data using accessor paths
TODO
* sandbox each client connection in separate environmentsso that they have their own separate root states
* predefined data shape/structure and validating update-data input against it; prevent setting data that doesn't match the schema; prevent replacing non-leaf nodes (maps), only allow updating leaves
* transform date strings into dates: https://github.com/metosin/malli/blob/0.2.1/src/malli/transform.cljc#L115
